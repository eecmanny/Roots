<!-- Loop over each object in the children array -->
{{#each children as |children| }}
<div class="row mb-4 children">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/children/{{children.id}}">{{children.first_name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount children.relative_to}}</span>
      needed!
    </p>

  </div>
  <div class="col-md-7">
    <p>
      {{children.gender}}
    </p>
  </div>
</div>
{{/each}}
